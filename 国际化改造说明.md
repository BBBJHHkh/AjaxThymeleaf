# Excel 模块国际化改造说明

## 改造概述

将 Excel 上传模块的所有硬编码文案统一到 `messages.properties` 中，实现前后端一体化的国际化支持。

---

## 改造内容

### 1. 配置文件

**文件：** `src/main/resources/messages.properties`（中文）

新增 **51 个** Excel 相关的国际化 key：

- 页面标题和说明（8 个）
- 按钮文本（3 个）
- 标签页标题（3 个）
- 表格标题和列名（15 个）
- JavaScript 验证消息（8 个）
- 错误消息（6 个）
- 状态文本（2 个）

**文件：** `src/main/resources/messages_en.properties`（英文）

同步添加所有 key 的英文翻译。

---

### 2. HTML 模板

**文件：** `src/main/resources/templates/excel-upload/excel-upload.html`

#### ① HTML 静态文案使用 Thymeleaf 国际化

```html
<!-- 改造前 -->
<h2>Excel 文件上传与校验</h2>

<!-- 改造后 -->
<h2 th:text="#{excel.page.title}">Excel 文件上传与校验</h2>
```

**改造点：**
- 页面标题
- 说明文本
- 按钮文本
- 标签页标题
- 表格标题和列名

所有静态文案均使用 `th:text="#{key}"` 语法。

#### ② 内联 JavaScript 消息常量

在 HTML 底部添加 Thymeleaf 内联脚本，将 `messages.properties` 的文案注入到 JavaScript 全局对象 `MSG`：

```html
<script th:inline="javascript">
    var MSG = {
        FILE_REQUIRED: /*[[#{excel.file.required}]]*/ '请先选择一个 Excel 文件',
        FILE_TOO_LARGE: /*[[#{excel.file.tooLarge}]]*/ '文件过大（{0} MB），最大允许 {1} MB',
        // ... 更多消息
    };
    
    function formatMsg(template, ...args) {
        var result = template;
        args.forEach(function(arg, index) {
            result = result.replace('{' + index + '}', arg);
        });
        return result;
    }
</script>
```

**特点：**
- 使用 Thymeleaf 内联语法 `/*[[#{key}]]*/`
- 保留中文作为默认值（在非 Thymeleaf 环境下也能正常显示）
- 提供 `formatMsg()` 函数支持参数化消息

---

### 3. JavaScript 业务逻辑

**文件：** `src/main/resources/static/js/excel-upload/excel-upload.js`

#### 改造内容

**改造前：**
```javascript
return '请先选择一个 Excel 文件';
```

**改造后：**
```javascript
return MSG.FILE_REQUIRED;
```

**所有改造点：**

| 函数/位置 | 改造数量 | 说明 |
|---|---|---|
| `validateFile()` | 3 处 | 文件验证消息 |
| `$('#btnCheck').click()` | 8 处 | 校验按钮文本和错误消息 |
| `$('#btnUpload').click()` | 4 处 | 上传按钮文本和错误消息 |
| `displayData()` | 4 处 | 表格状态标签 |

**参数化消息示例：**
```javascript
// 改造前
return '文件过大（' + sizeMB + ' MB），最大允许 20 MB';

// 改造后
return formatMsg(MSG.FILE_TOO_LARGE, sizeMB, maxMB);
```

---

## 技术亮点

### ✅ 1. 前后端统一配置

- 后端 Java 代码和前端 JavaScript 都从同一个 `messages.properties` 读取
- 避免配置分散，维护成本低

### ✅ 2. Thymeleaf 内联技术

- 使用 `th:inline="javascript"` 将后端配置注入到前端
- 保持了 JavaScript 代码的独立性（可以单独加载）

### ✅ 3. 参数化消息

- 支持 `{0}`, `{1}` 占位符
- `formatMsg()` 函数统一处理参数替换

### ✅ 4. 多语言支持

- 已配置中文（`messages.properties`）和英文（`messages_en.properties`）
- Spring 根据浏览器语言自动切换（`Accept-Language` 头）

### ✅ 5. 向后兼容

- Thymeleaf 内联保留了默认中文值
- 即使在非 Spring 环境下，JavaScript 也能正常工作

---

## 使用方式

### 切换语言

#### 方法 1：浏览器语言设置

浏览器会自动发送 `Accept-Language` 头，Spring 自动识别：

```
Accept-Language: zh-CN  → 显示中文
Accept-Language: en-US  → 显示英文
```

#### 方法 2：URL 参数（可选）

如果配置了 `LocaleChangeInterceptor`，可以通过 URL 参数切换：

```
http://localhost:8080/thymeleaf/excel-upload?lang=zh
http://localhost:8080/thymeleaf/excel-upload?lang=en
```

---

## 文件清单

| 文件 | 改动内容 | 行数变化 |
|---|---|---|
| `messages.properties` | 新增 51 个 Excel 相关 key | +51 行 |
| `messages_en.properties` | 新增 51 个英文翻译 | +51 行 |
| `excel-upload.html` | 所有静态文案改为 `th:text`，新增内联脚本 | 140 → 179 行 |
| `excel-upload.js` | 所有硬编码字符串改为 `MSG.XXX` | 无增减（215 行） |

---

## 验证清单

### ✅ 已验证项

1. **JavaScript 无硬编码** — 通过 `grep` 搜索，确认无中文硬编码
2. **HTML 使用国际化** — 所有文案都使用 `th:text="#{key}"`
3. **消息常量完整** — `MSG` 对象包含所有需要的消息
4. **参数化支持** — `formatMsg()` 函数正常工作

### 🔍 建议测试

1. **中文环境测试** — 浏览器设置为中文，验证所有文案显示正常
2. **英文环境测试** — 浏览器设置为英文，验证翻译准确性
3. **功能测试** — 验证文件上传、校验、错误提示功能正常
4. **边界测试** — 验证参数化消息（文件大小、错误代码）显示正确

---

## 扩展多语言

### 添加新语言（如日语）

1. 创建 `messages_ja.properties`：
```properties
excel.page.title=Excelファイルのアップロードと検証
excel.file.required=Excelファイルを選択してください
# ... 其他翻译
```

2. 无需修改代码，Spring 会自动识别 `Accept-Language: ja`

---

## 注意事项

⚠️ **缓存问题**

- 开发期：Spring DevTools 会自动禁用浏览器缓存
- 生产期：修改 `messages.properties` 后需要**重启应用**才能生效
- 修改 `excel-upload.js` 后，浏览器会根据 ETag 自动刷新

⚠️ **占位符格式**

- `messages.properties` 使用 `{0}`, `{1}`
- JavaScript 的 `formatMsg()` 函数会自动替换
- 注意参数顺序必须匹配

---

## 总结

✅ **改造完成！** Excel 模块已全面国际化：

- **51 个国际化 key** 统一管理
- **HTML 静态文案** 全部使用 Thymeleaf 国际化
- **JavaScript 动态文案** 通过内联脚本从后端获取
- **中英文双语** 支持，可轻松扩展其他语言
- **零硬编码** — 所有文案集中在 `messages.properties`

**架构优势：**
- 前后端统一配置
- 易于维护和扩展
- 符合 Spring 最佳实践
